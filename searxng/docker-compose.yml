version: "3.8"

services:
  searxng:
    image: searxng/searxng:latest
    networks:
      - caddy
    environment:
      SEARXNG_BASE_URL: "https://buscador.semudando.com.br"
    labels:
      caddy: buscador.semudando.com.br
      caddy.reverse_proxy: "{{upstreams 8080}}"


networks:
  caddy:
    external: true